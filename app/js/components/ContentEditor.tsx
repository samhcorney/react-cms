import * as React from 'react';

export class ContentEditor extends React.Component<any, {}> {

    constructor( props : any ) {
        super( props );
    }

    render() {
        return (
            <p>Content editor content</p>
        );
    }
}
